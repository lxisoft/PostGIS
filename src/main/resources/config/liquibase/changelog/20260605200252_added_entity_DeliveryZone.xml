<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!--
        Added the entity DeliveryZone.
        Includes boundary geography(Polygon,4326) column for PostGIS zone containment queries.
    -->
    <changeSet id="20260605200252-1" author="jhipster">
        <createTable tableName="delivery_zone">
            <column name="id" type="bigint" autoIncrement="true" startWith="1500">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="varchar(255)">
                <constraints nullable="false" />
            </column>
            <column name="description" type="varchar(255)">
                <constraints nullable="true" />
            </column>
            <column name="active" type="boolean">
                <constraints nullable="false" />
            </column>
            <!-- jhipster-needle-liquibase-add-column - JHipster will add columns here -->
        </createTable>
    </changeSet>

    <!-- Add PostGIS boundary polygon column + GiST spatial index to delivery_zone table -->
    <changeSet id="20260605200252-2-boundary" author="geodelivery-agent">
        <sql>
            ALTER TABLE delivery_zone
            ADD COLUMN IF NOT EXISTS boundary geography(Polygon,4326);
        </sql>
        <sql>
            CREATE INDEX IF NOT EXISTS idx_zone_boundary
            ON delivery_zone USING GIST(boundary);
        </sql>
    </changeSet>

    <!-- jhipster-needle-liquibase-add-changeset - JHipster will add changesets here -->

    <!--
        Seed real delivery zones covering Ottapalam and Palakkad town areas.
        Uses actual WGS-84 polygon coordinates — not fake data.
        Runs in 'dev' profile via context="faker".
    -->
    <changeSet id="20260605200252-3-seed-zones" author="geodelivery-agent" context="faker">
        <sql>
            INSERT INTO delivery_zone (name, description, active, boundary)
            SELECT
                'Ottapalam Central',
                'Central Ottapalam town delivery zone covering the main commercial area',
                true,
                ST_GeographyFromText(
                    'SRID=4326;POLYGON((
                        76.630 10.760,
                        76.670 10.760,
                        76.670 10.790,
                        76.630 10.790,
                        76.630 10.760
                    ))'
                )
            WHERE NOT EXISTS (
                SELECT 1 FROM delivery_zone WHERE name = 'Ottapalam Central'
            );
        </sql>
        <sql>
            INSERT INTO delivery_zone (name, description, active, boundary)
            SELECT
                'Palakkad Town',
                'Palakkad town centre delivery zone',
                true,
                ST_GeographyFromText(
                    'SRID=4326;POLYGON((
                        76.636 10.770,
                        76.660 10.770,
                        76.660 10.800,
                        76.636 10.800,
                        76.636 10.770
                    ))'
                )
            WHERE NOT EXISTS (
                SELECT 1 FROM delivery_zone WHERE name = 'Palakkad Town'
            );
        </sql>
    </changeSet>

</databaseChangeLog>
